package ax.std.axis;

public enum SnapPolicy {

    /** Snapping is disabled */
    DISABLED,
    
    /** Snapping is enabled and snaps to global values */
    GLOBAL,
    
    /** Snapping is enabled and snaps to values relative to the current objects value */
    RELATIVE,
    
}
